# tests/backend — aggregator. Subdirectories are wired in below; this
# directory has no source of its own.

add_subdirectory(common)
add_subdirectory(tier1)
add_subdirectory(tier2)
add_subdirectory(shim)
